---
import photo from "../assets/MunicipalTapWater.webp";
---

<section class="text-white">
        <div class="mx-auto max-w-5xl px-4 py-8 md:py-10">

          <!-- Contenedor para imagen superpuesta -->
          <div class="relative">
            
            
          <!-- Banda azul detrás para dar sensación de superposición -->
          <div class="group relative h-40 w-[88%] -ml-2 md:-ml-6 rounded-2xl bg-[color:var(--accent)]/95 overflow-hidden">
            <!-- Brillo (se desliza al hover) -->
        <span class="pointer-events-none absolute inset-0 bg-gradient-to-r from-transparent via-white/30 to-transparent -translate-x-[120%] transition-transform duration-[1600ms] ease-out group-hover:translate-x-[120%]"></span>
            <div class="p-10">
            <p class="font-semibold text-3xl md:text-5xl">Source: Municipal Tap Water</p>
            </div>
          </div>

          <!-- Imagen (superpuesta sobre la banda) -->
          <figure
            class="relative z-10 -mt-10 shadow-xl overflow-hidden border border-black/5"
          >
            <img
              src={photo.src}
              alt="HSW bottles"
              class="block w-full h-[360px] md:h-[420px] object-cover"
              loading="eager"
            />

            <!-- “resaltador” brillante como en la maqueta (opcional)
        <div
          class="absolute right-[14%] top-[18%] h-40 w-36 rounded-2xl ring-4 ring-white/80 shadow-2xl
                     backdrop-blur-[1px] bg-white/10"></div> -->
          </figure>
        </div>

        <!-- Texto simulado abajo -->
        <div class="mt-8 text-xl leading-relaxed text-gray-700">
          <p>
            Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do
            eiusmod tempor incididunt ut labore et dolore magna aliqua. Quis
            ipsum suspendisse ultrices gravida.
          </p>
        </div>
      </section>